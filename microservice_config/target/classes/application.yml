server:
  port: 7101
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka
  instance:
    hostname: localhost
spring:
  application:
    name: microservice-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yutenghui/microservice-config.git